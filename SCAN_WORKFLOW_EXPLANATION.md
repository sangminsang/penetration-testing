# 스캔 워크플로우 상세 설명

## 🎯 이상적인 스캔 흐름 (개선 완료)

```
┌─────────────────────────────────────────────────────────┐
│ Step 1: Katana 크롤링 (URL 수집)                        │
│ - 전체 사이트 구조 파악                                  │
│ - 모든 엔드포인트 발견                                    │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ Step 2: Nuclei 스캔 (취약점 후보 탐지)                   │
│ - 발견된 모든 URL에 대해 취약점 스캔                      │
│ - 기술 스택 탐지 (tech 태그)                             │
│ - CVE, 취약점, 노출 정보 탐지                             │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ Step 3: 필터링 (취약점 발견된 URL만 추출)                │
│ - Nuclei 결과에서 취약점이 발견된 URL만 추출              │
│ - 중복 제거 및 정리                                       │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ Step 4: ZAP Targeted Scan (취약한 URL만 정밀 검증)      │
│ - 취약점 발견된 URL만 ZAP으로 정밀 검증                    │
│ - 중복 크롤링 없이 즉시 Active Scan 수행                  │
│ - False Positive 제거                                     │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ Step 5: VulnerabilityVerifier (실제 검증)               │
│ - 민감 경로 체크 (/admin, /.git 등)                      │
│ - 실제 HTTP 요청으로 취약점 확인                          │
│ - 컨텍스트 기반 오탐 제거                                  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔍 Nuclei vs WhatWeb 비교

### **Nuclei의 장점:**
1. **취약점 탐지**: CVE, 취약점, 노출 정보를 직접 탐지
2. **빠른 속도**: 대량의 URL을 빠르게 스캔 가능
3. **기술 스택 탐지**: `tech-detect` 태그로 기술 스택 탐지 가능
4. **활발한 커뮤니티**: 지속적으로 업데이트되는 템플릿

### **WhatWeb의 장점:**
1. **정밀한 버전 탐지**: 더 정확한 버전 정보 제공
2. **메타데이터**: IP, 국가, 서버 정보 등 추가 정보
3. **안정성**: 오래된 도구로 검증됨

### **현재 구현 전략:**
- **Nuclei를 주력으로 사용**: 빠르고 취약점 탐지에 특화
- **WhatWeb을 보완으로 사용**: Nuclei가 버전 정보를 제공하지 않을 때 보완
- **중복 제거**: Nuclei에서 이미 발견한 기술은 WhatWeb에서 제외

---

## 🐳 Docker 워크플로우 개선 사항

### **이전 (개선 전):**
```
Katana → Nuclei → 끝
```

### **현재 (개선 후):**
```
Katana → Nuclei → 필터링 → ZAP Targeted Scan → VulnerabilityVerifier
```

### **주요 개선점:**
1. ✅ **ZAP 검증 추가**: 취약점 발견된 URL만 정밀 검증
2. ✅ **False Positive 감소**: ZAP과 VulnerabilityVerifier로 이중 검증
3. ✅ **효율성 향상**: 모든 URL을 스캔하지 않고 취약한 URL만 검증
4. ✅ **정확도 향상**: 다단계 검증으로 오탐 제거

---

## ⚙️ 설정 요구사항

### **Docker 환경에서 ZAP 접근:**
현재 `config.py`에서 ZAP 설정:
```python
ZAP_PROXY_HOST = '127.0.0.1'  # Docker 컨테이너에서는 'zap' 또는 호스트 IP 필요
ZAP_PROXY_PORT = 8080
```

**Docker Compose 네트워크 사용 시:**
- `docker-compose.yml`에 ZAP 서비스가 있으면 `ZAP_PROXY_HOST = 'zap'`로 설정
- 같은 네트워크에 있으면 서비스 이름으로 접근 가능

**독립 실행 시:**
- 호스트의 ZAP에 접근하려면 `ZAP_PROXY_HOST = 'host.docker.internal'` (Windows/Mac)
- 또는 `ZAP_PROXY_HOST = '172.17.0.1'` (Linux Docker 기본 게이트웨이)

---

## 📊 결과 데이터 구조

### **collect_web_info() 반환값:**
```python
{
    'headers': {},                    # HTTP 헤더
    'webtechnologies': [],            # 기술 스택 (Nuclei + WhatWeb)
    'nuclei_vulns': [],              # Nuclei 발견 취약점
    'zap_results': {                 # ZAP 검증 결과
        'alerts': [],
        'scanned_urls': 0
    },
    'verifications': []              # VulnerabilityVerifier 검증 결과
}
```

---

## 🚀 사용 예시

### **Docker 워커에서:**
```python
from app.core.recon.web import collect_web_info

result = collect_web_info("http://target.com")
# 자동으로 이상적인 흐름 실행:
# 1. Katana 크롤링
# 2. Nuclei 스캔
# 3. 필터링
# 4. ZAP Targeted Scan
# 5. VulnerabilityVerifier
```

---

## ⚠️ 주의사항

1. **ZAP 접근성**: Docker 컨테이너에서 ZAP에 접근할 수 있어야 함
2. **성능**: 취약점이 많은 경우 ZAP 스캔 시간이 길어질 수 있음
3. **리소스**: ZAP은 메모리를 많이 사용하므로 충분한 리소스 필요
4. **타임아웃**: 각 단계별 타임아웃 설정으로 무한 대기 방지

---

## 🔄 향후 개선 가능 사항

1. **병렬 처리**: 여러 URL을 동시에 ZAP 스캔
2. **캐싱**: 이미 스캔한 URL은 캐시에서 가져오기
3. **우선순위**: Critical/High 취약점부터 우선 검증
4. **리포트**: 상세한 검증 리포트 생성

