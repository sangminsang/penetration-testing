# Flask 웹 프레임워크
Flask>=3.0.0

# 네트워크 스캐닝
python-nmap>=0.7.1  # import nmap으로 사용

# HTTP 요청
requests>=2.31.0

# 버전 비교 (CVE 매칭용)
packaging>=23.0

# 타입 힌팅 (Python 3.8 이하에서 필요할 수 있음)
typing-extensions>=4.5.0; python_version<"3.9"

# 데이터베이스 스캐닝 (선택적 - 상세 분석용)
pymysql>=1.0.0  # MySQL 인증 테스트 (core/recon/database.py에서 사용)
psycopg2-binary>=2.9.0  # PostgreSQL 인증 테스트 (core/recon/database.py에서 사용)
pymongo>=4.0.0  # MongoDB 상세 분석 (core/recon/database.py에서 사용)
redis>=4.0.0  # Redis 상세 분석 (core/recon/database.py, core/cve/cache_manager.py에서 사용)

# FTP 분석 (표준 라이브러리 ftplib 사용)
# SNMP 분석 (시스템에 snmpwalk 설치 필요)

# AWS SDK (S3 버킷 정책 분석용 - 선택적)
# boto3>=1.28.0  # AWS S3 버킷 정책 분석

# 진행 상황 표시 (멀티스레딩용)
tqdm>=4.66.0

# 취약점 검증용 (선택적)
# requests>=2.31.0  # 이미 있음
PyJWT>=2.8.0  # JWT 취약점 테스트용
urllib3>=2.0.0  # 연결 풀링 및 재시도용
boto3>=1.28.0  # AWS 클라우드 보안 테스트용 (선택적)
cvss>=3.1.0  # CVSS 3.1 계산 라이브러리 (선택적)

# 비동기 HTTP 클라이언트 (NVD API 비동기 호출용)
aiohttp>=3.9.0  # 비동기 HTTP 요청 (async_nvd_client.py에서 사용)

Flask-SQLAlchemy==3.1.1
